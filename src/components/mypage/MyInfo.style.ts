import styled from "styled-components";

export const MyInfoContainer = styled.div`
  z-index: 1;
  position: absolute;
  text-align: center;
  bottom: 150px;
  .infoTitle {
    font-size: 16px;
    font-weight: 700;
    margin: 22px 0 22px 0;
  }
`;
