import React from "react";
import styled from "styled-components";

const ProfileBarContainer = styled.div`
  width: 500px;
  border: 1px solid black;
  height: 100vh;
`;

const ProfileBar = () => {
  return <ProfileBarContainer></ProfileBarContainer>;
};

export default ProfileBar;
